"""Phase 5 — Intelligent Edit & Undo Agent (placeholder).

Will contain:
  • intent_classifier.py — LLM classifies free-text edit query → structured intent
  • planner.py           — Plans which phase(s) to re-run for the intent
  • executor.py          — Calls MCP tools to apply the edit
  • agent.py             — Orchestrates classify → plan → execute → snapshot
"""
